spring:
  application:
    name: servicio-zuul-server
  cloud:
    config:
      uri: http://localhost:8888
    
management:
  endpoints:
    web:
      exposure:
        include: "*"

---

spring:
  profiles: docker
  cloud:
    config:
      uri: http://config-server:8888   
    